<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="LoanShark.View.BankAccountListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LoanShark.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Content (fills remaining space) -->
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Padding="20" Grid.Row="0" Margin="0,0,0,30" BorderBrush="Gray" BorderThickness="0,0,0,4">
            <Grid Height="50">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <!-- Button Column -->
                    <ColumnDefinition Width="*"/>
                    <!-- Header Column -->
                </Grid.ColumnDefinitions>

                <Button Content="To Main Page" Width="Auto" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0"/>
                <TextBlock Text="Bank Accounts" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="24" FontWeight="Bold" Grid.ColumnSpan="2"/>
            </Grid>
        </Border>

        <!-- Content Section (Centered Vertically) -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Select currency:" Margin="10"/>

            <!-- ✅ Border Around List -->
            <Border BorderBrush="Gray" BorderThickness="2" CornerRadius="5" Padding="2" Margin="10">
                <ListView ItemsSource="{x:Bind Items}" Margin="0" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:ItemModel">
                            <!-- ✅ Border Around Each Item -->
                            <Border BorderBrush="Gray" BorderThickness="0,0,0,1" Padding="5">
                                <StackPanel Orientation="Horizontal" Margin="5">
                                    <TextBlock MinWidth="200" Text="{x:Bind Name}" 
                                             Margin="5"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>

            <!-- Confirm Button -->
            <Button Margin="0,10,0,10" HorizontalAlignment="Center" Content="Confirm creation" Width="Auto" Height="Auto"/>
        </StackPanel>
    </Grid>
</Window>
