<Page
    x:Class="LoanShark.View.SendMoneyView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LoanShark.ViewModel">

    <Page.DataContext>
        <local:SendMoneyViewModel/>
    </Page.DataContext>

    <Grid Padding="20">
        <!-- Page Header with Back Button -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
            <Button Content="&#x2190;" Command="{Binding BackCommand}"
                    FontSize="24" Width="50" Height="40"
                    HorizontalAlignment="Left" Background="Transparent" BorderThickness="0"/>
            <TextBlock Text="Send Money" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Input Fields -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300" Spacing="10">
            <TextBlock Text="Receiver's IBAN:" FontSize="18"/>
            <TextBox Text="{Binding IBAN, Mode=TwoWay}" PlaceholderText="Enter IBAN"/>

            <TextBlock Text="Sum of Money:" FontSize="18"/>
            <TextBox Text="{Binding SumOfMoney, Mode=TwoWay}" PlaceholderText="Enter amount"/>

            <TextBlock Text="Details:" FontSize="18"/>
            <TextBox Text="{Binding Details, Mode=TwoWay}" PlaceholderText="Payment details"/>

            <!-- Pay Button -->
            <Button Content="Pay" Command="{Binding PayCommand}" Width="300" Height="40" Background="Green" Foreground="White"/>

            <!-- Back Button -->
            <Button Content="Back" Command="{Binding BackCommand}" Width="300" Height="40" Background="Gray" Foreground="White"/>
        </StackPanel>
    </Grid>
</Page>
