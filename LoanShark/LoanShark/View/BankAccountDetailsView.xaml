<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="LoanShark.View.BankAccountDetailsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LoanShark.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <!-- Content (fills remaining space) -->
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Padding="20" Grid.Row="0" Margin="0,0,0,30" BorderBrush="Gray" BorderThickness="0,0,0,4">
            <TextBlock Text="Bank Account Details" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="24" FontWeight="Bold" Grid.ColumnSpan="2"/>
        </Border>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Currency:"/>
            <TextBox Text="{Binding BankAccount.currency}" MinWidth="300" PlaceholderText="Placeholder" Margin="10" IsReadOnly="True" IsHitTestVisible="False"/>
            <TextBlock Text="Custom name:"/>
            <TextBox Text="{Binding BankAccount.name}" PlaceholderText="Placeholder" Margin="10" IsReadOnly="True" IsHitTestVisible="False"/>
            <TextBlock Text="Sold:"/>
            <TextBox Text="{Binding BankAccount.sold}" PlaceholderText="Placeholder" Margin="10" IsReadOnly="True" IsHitTestVisible="False"/>
            <TextBlock Text="IBAN:"/>
            <TextBox Text="{Binding BankAccount.iban}" PlaceholderText="Placeholder" Margin="10" IsReadOnly="True" IsHitTestVisible="False"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Blocked:" VerticalAlignment="Center" Margin="0,0,20,0"/>
                <CheckBox IsEnabled="{Binding BankAccount.blocked}"/>
            </StackPanel>
        </StackPanel>

        <Button Grid.Row="2" Command="{Binding ButtonCommand}" Content="Back" Width="100" BorderBrush="AliceBlue" Margin="0,0,0,30" HorizontalAlignment="Center"/>
    </Grid>
</Window>
